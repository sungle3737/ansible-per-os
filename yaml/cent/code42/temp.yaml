---
  - name: mount and startup
    hosts: localhost
    gather_facts: no
  
    tasks:
      - name: ensure a boot script exists
        local_action:
          module: vultr_startup_script
          name: vultr_startup_script
          script: rm /var/www/pj/pj