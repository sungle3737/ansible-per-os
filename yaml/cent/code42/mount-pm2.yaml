---
- name: Start apache and register startup mariadb
  hosts: cents
  gather_facts: no
  yum install epel-release -y
  yum install nodejs -y
  npm i -g pm2
  # register svr_root
  SVR_ROOT=/mnt/nas/smms_output
  pm2 serve $SVR_ROOT 58022 --force --name "nassvr_code42_1"
  # register startup
  pm2 startup


  curl https://raw.githubusercontent.com/creationix/nvm/v0.35.2/install.sh | bash
  source ~/.bashrc
  nvm install node